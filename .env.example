# 仅放环境差异与密钥；其余默认值见 api/config/settings.py
# 复制本文件为 .env 后填写

# 对外监听（本地默认可不设，保持 127.0.0.1）
# API_HOST=0.0.0.0

# 本地直连 compose 的 db（宿主机映射 5433）；容器内 api 仍用 db:5432，无需改
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5433/recording_transcription

LLM_API_KEY=
