### 录音转写服务 API 调试
### 需先启动服务：http://127.0.0.1:8000
@baseUrl = http://127.0.0.1:8000

### 健康检查
GET {{baseUrl}}/health

### 上传录音（请改成本地真实音频路径）
# @name upload
POST {{baseUrl}}/v1/recordings
Content-Type: multipart/form-data; boundary=----Boundary

------Boundary
Content-Disposition: form-data; name="file"; filename="demo.mp3"
Content-Type: audio/mpeg

< ./uploads/demo.mp3
------Boundary--

### 从上传响应取出 id
@recordingId = {{upload.response.body.recording_id}}
@taskId = {{upload.response.body.task_id}}

### 查询任务状态（processing 中 status 为当前阶段）
GET {{baseUrl}}/v1/tasks/{{taskId}}

### 录音列表（按创建时间倒序，含任务状态）
GET {{baseUrl}}/v1/recordings?page=1&page_size=20

### 录音详情（done 时含 transcript 与 summary）
GET {{baseUrl}}/v1/recordings/{{recordingId}}

### 失败任务重试（仅 failed；重复请求幂等）
POST {{baseUrl}}/v1/tasks/{{taskId}}/retry

### 删除录音（含文件与关联任务）
DELETE {{baseUrl}}/v1/recordings/{{recordingId}}
